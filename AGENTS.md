# mycodex 协作规则

本仓库是 PPW 的个人 Codex 配置与技能仓库。

## 修改规则

- 默认中文说明和中文文档。
- 先读仓库结构、README、脚本、配置和现有 skill，再决定修改方式；不要凭目录名臆测用途。
- 技能数量宁少勿多，新增前先判断是否可以并入现有 skill。
- 活跃 skill 放 `skills/<name>/SKILL.md`。
- 长参考资料放 `skills/<name>/references/` 或 `references/<source>/`。
- 常驻协作习惯、验证闭环、最小修改和仓库维护规则放 `AGENTS.md`；不要塞进某个总入口 skill。
- Skill 只放可触发的专项能力、领域流程、工具路由或模板导航；避免把普通编码规则包装成默认 skill。
- `ppw-codex-engineer` 只作为个人工程路由和项目脚手架入口，不作为普通实现、修复、审查的默认入口。
- 不提交 `auth.json`、`.env`、SQLite、日志、shell 快照。
- 修改后运行 `./scripts/linux/verify.sh`。

## 项目分析原则

- 初始化或重排本仓库时，先识别 `skills/`、`references/`、`config/`、`docs/`、`scripts/` 的边界和安装关系。
- 分析新增资料来源时，区分 active skill、参考资料、内部维护记录和 ignored 本地缓存。
- 新增或恢复 skill 前，明确触发条件、与现有 skill 的重叠、是否需要外部授权、是否会增加噪声。
- 对外部仓库或 zip，只吸收可复用流程和模板；不要复制个人路径、Claude 专用运行时、agents/openai.yaml、hooks 或用户页面入口。
- 对不确定内容标注来源和限制；无法验证时保守处理，不把猜测写成仓库规则。

## 最小护栏选择

- 小改动默认不增加流程；如果触及数据、鉴权、prompt、公开 API 或架构边界，先做风险判断。
- Setup/tooling 不新增框架、包管理器、服务、缓存、插件或运行时，除非现有栈无法完成当前任务。
- Feature delivery 先确认现有架构能否承载；避免把新职责塞进临近文件，必要时先抽出最小模块边界。
- Bug fix 先复现或定位证据，再修最小根因；高复发路径要补回归测试。
- Refactor 默认保持行为，不混入无关格式化、依赖升级或新功能。
- 高风险歧义先问清：敏感数据、auth、payment、prompt safety、公开 API、数据迁移/删除/覆盖。
- 若实现牺牲了安全、隐私、性能、可靠性、可观测性、测试深度、可访问性或长期扩展性，最终说明取舍和后续改进点。

## 系统排障规则

- 不在未理解根因前猜测式修复；先读完整错误、堆栈、日志、失败命令和相关配置。
- 能复现就先复现，记录最小复现步骤；不能稳定复现时先补充诊断信息，不直接改逻辑赌结果。
- 先查最近变更、依赖、环境变量、配置、数据流和组件边界，定位问题发生在哪一层。
- 对多组件问题，先在边界采证：输入、输出、环境传递、状态变化，再决定修复点。
- 一次只验证一个假设，用最小改动测试；失败后回到证据和假设，不叠加多个“可能修复”。
- 连续 3 次修复失败时停止继续打补丁，重新评估架构、耦合或需求假设，并向用户说明卡点。

## 验证前置

- 没有新鲜验证证据，不声称“完成、修好、通过、没问题”。
- 提交、推送或最终回复前，先识别能证明结果的命令，完整运行，读取输出和退出码。
- 验证失败时报告真实状态和失败信息，不用“应该可以”“看起来可以”替代证据。
- 对本仓库，默认验证命令是 `./scripts/linux/verify.sh`；如改动类型需要额外测试，按实际项目补充对应命令。

## 提交规则

- 使用语义化提交信息。
- 一次提交只做一类变更：新增 skill、裁剪 skill、更新资料、脚本调整。
- 提交前检查 `git status --short` 和暂存区，避免把 `config/codex/config.toml` 等用户本地改动混入无关提交。

## 工具使用原则

- 离线能完成的仓库维护任务，优先用本地文件、`rg`、`find`、`git diff`、验证脚本。
- 查询外部项目、最新插件、官方文档或用户给出的 URL 时，再使用 web 或文档工具，并优先官方/一方来源。
- 技术文档查询优先 Context7 或官方文档；OpenAI 产品问题优先官方 OpenAI 文档。
- 工具调用保持最小必要：先明确问题，再查最小范围，避免大规模抓取无关资料。
- 引用网络或外部资料形成长期规则时，优先沉淀到 `docs/` 或对应 skill 的 `references/`，不要散落在正文描述里。

## 文档规范

- 面向用户的入口文档保持简洁：`README.md` 只放安装、当前能力和维护入口。
- 内部来源、裁剪记录和历史决策放 `.maintenance/`，不要暴露为用户入口。
- `docs/` 放可供用户或未来维护者阅读的指南和能力盘点。
- `SKILL.md` 保持短而可执行；长模板、完整上游说明和许可证放 `references/`。
- 代码注释和文档默认中文；代码标识符遵循项目语言和已有风格，不强行中文化。
